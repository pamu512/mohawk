[package]
name = "mohawk-fsrs"
version = "0.1.0"
edition = "2021"
description = "FSRS scheduling calculations used by Mohawk"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
